import { PRACTICE_COURSES } from '../../lib/math/practice';

export function CourseReferencePage() {
  return <main className="workspace practice-page reference-page"><section className="practice-hero"><div><span className="section-kicker">Curriculum reference</span><h1>Course Reference</h1><p>The learning map mirrors MathLab’s deterministic capabilities. Practice topics intentionally stay inside mathematics that the current engine can compute or verify reliably.</p></div></section><section className="reference-course-list">{PRACTICE_COURSES.map((course)=><article key={course.id}><div><span>{course.phaseRange}</span><h2>{course.title}</h2><p>{course.description}</p></div><div>{course.topics.map((topic)=><section key={topic.id}><strong>{topic.title}</strong><p>{topic.description}</p><span>{topic.templateIds.length} generated template{topic.templateIds.length===1?'':'s'} · {topic.authoredIds.length} authored exercise{topic.authoredIds.length===1?'':'s'}</span></section>)}</div></article>)}</section><section className="reference-boundary"><strong>Learning-system boundary</strong><p>Generated questions do not outrun the underlying engine. If a topic cannot be checked deterministically by the current mathematics/proof stack, MathLab does not grade it heuristically as correct.</p></section></main>;
}
